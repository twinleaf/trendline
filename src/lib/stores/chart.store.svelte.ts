export type ChartLayout = 'carousel' | 'vertical' | 'horizontal';

export type StreamLayout = 'grouped' | 'vertical' | 'horizontal';

// MAKE THREE GROUPS

// ONE FOR THE CHART WINDOW LAYOUT

// ANOTHER FOR STREAMS

// WRITE HELPERS THAT, PER DEVICE, MAY COMBINE ALL STREAM COLUMNS OF SAME UNITS (GROUPED) OR SPLIT THE STREAM COLUMNS (VERTICALLY OR HORIZONTALLY)